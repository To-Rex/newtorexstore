name: torexstore
description: "Torex Local Storage - Test and benchmark application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Torex Local Storage package (mono repo)
  torex_local_store:
    path: TorexLocalStore


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
